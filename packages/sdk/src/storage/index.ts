/**
 * KeyGuard SDK - Storage Adapters
 * 
 * Secure storage implementations:
 * - Browser: IndexedDB via idb-keyval
 * - Node.js: System Keychain (Phase 2)
 */

export { BrowserStorageAdapter } from './browser';
